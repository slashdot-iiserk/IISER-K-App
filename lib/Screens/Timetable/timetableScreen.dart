import 'package:flutter/material.dart';
import 'package:iiserk_app/Pages/Home/screen.dart';

final timetableScreen = Screen(
  'Timetable',
  (BuildContext context) {
    return Container(
      child: Center(
        child: Text('Timetable Screen'),
      ),
    );
  },
);
